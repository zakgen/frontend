import { MockDashboardApi } from "@/lib/api/adapters/mock";

// Placeholder adapter for the later backend-integration phase.
// The UI-first V0 runs entirely on the mock adapter.
export class RestDashboardApi extends MockDashboardApi {}
